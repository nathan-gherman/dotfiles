monitor = eDP-1, 1920x1080, 0x0, 1
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia

$terminal = ghostty
$fileManager = nautilus
$menu = wofi --show drun
#$menu = rofi -show drun
$web = flatpak run app.zen_browser.zen

exec-once = waybar & hyprpaper &
#exec-once = hyprpm reload -n

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

#permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

general {
    gaps_in = 2
    gaps_out = 3

    border_size = 3

    col.active_border = rgba(8caaeeaa)
    col.inactive_border = rgba(595959aa)

    resize_on_border = false

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 7
    #rounding_power = 3

    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = false
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

animations {
    enabled = no, please :)


    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}


dwindle {
    pseudotile = true 
    preserve_split = true 
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0 
    disable_hyprland_logo = true 
}

input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0

    touchpad {
        natural_scroll = false
    }
}

gestures {
    workspace_swipe = false
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

$mainMod = SUPER 

bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, R, exec, $menu
bind = $mainMod, W, exec, $web
bind = $mainMod  SHIFT, R, exec, if pgrep -x waybar > /dev/null; then pkill waybar && waybar & else waybar & fi
bind = $mainMod, P, exec, hyprpicker -a
bind = $mainMod, Q, killactive,
bind = $mainMod  SHIFT, Q, exit,
bind = $mainMod, V, togglefloating,
bind = $mainMod, F, fullscreen,
bind = $mainMod, D, fullscreen, 1
bind = $mainMod, J, togglesplit, # dwindle

# Fullscreen screenshot with Print
bind = $mainMod, Print, exec, wayshot -f ~/Pictures/Screenshots/screenshot_$(date +%Y-%m-%d_%H-%M-%S).png

# Area selection screenshot with Shift+Print
bind = $mainMod SHIFT, Print, exec, wayshot -s "$(slurp)" -f ~/Pictures/Screenshots/screenshot_area_$(date +%Y-%m-%d_%H-%M-%S).png

# Move focus with mainMod + arrow keys
bind = $mainMod, left,  movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up,    movefocus, u
bind = $mainMod, down,  movefocus, d

# Move active window
bind = $mainMod CTRL, left,  movewindow, l
bind = $mainMod CTRL, down,  movewindow, d
bind = $mainMod CTRL, up,    movewindow, u
bind = $mainMod CTRL, right, movewindow, r

# Resize active window
bind = $mainMod SHIFT, right, resizeactive, 10 0
bind = $mainMod SHIFT, left, resizeactive, -10 0
bind = $mainMod SHIFT, up, resizeactive, 0 -10 
bind = $mainMod SHIFT, down, resizeactive, 0 10

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF87MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF87MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

#bindl = ,XF86AudioNext, exec, playerctl next
#bindl = ,XF86AudioPause, exec, playerctl play-pause
#bindl = ,XF86AudioPlay, exec, playerctl play-pause
#bindl = ,XF86AudioPrev, exec, playerctl previous

bind = ALT, SPACE, exec, playerctl play-pause
bind = ALT, N, exec, playerctl next
bind = ALT, P, exec, playerctl previous

windowrule = float,center,class:kitty
windowrule = float,center,class:Alacritty
windowrule = float,center,class:Ghostty
windowrule = float,center,class:org.gnome.Nautilus
windowrule = float,center,class:org.gnome.Loupe
windowrule = float,center,class:org.gnome.Sly
windowrule = float,center,class:org.gnome.Papers
windowrule = float,center,class:Emacs
windowrule = float,center,class:nmgui-bin
windowrule = float,center,class:xdg-desktop-portal-gtk
windowrule = float,center,class:io.gitlab.daikhan.stable
windowrule = float,center,class:page.kramo.Cartridges
windowrule = float,center,class:io.gitlab.adhami3310.Impression
windowrule = float,center,class:io.github.seadve.Mousai
windowrule = float,center,class:org.gnome.World.PikaBackup
windowrule = float,center,class:org.nickvision.tubeconverter
windowrule = float,center,class:page.kramo.Sly
windowrule = float,center,class:io.github.giantpinkrobots.varia
windowrule = float,center,class:garden.jamie.Morphosis
windowrule = float,center,class:com.github.jeromerobert.pdfarranger
windowrule = float,center,class:com.github.hugolabe.Wike
windowrule = float,center,class:com.github.johnfactotum.Foliate
windowrule = float,center,class:io.bassi.Amberol
windowrule = float,center,class:dev.alextren.Spot
windowrule = float,center,class:io.github.flattool.Warehouse
windowrule = float,center,class:me.iepure.Ticketbooth
windowrule = float,center,class:com.github.jkotra.unlockr

windowrule = suppressevent maximize, class:.*
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
